<% if @insights.present? %>
  <section aria-label="<%= t('dashboard.v2.insights_label', default: 'Actionable insights') %>" class="mb-8">
    <div class="flex flex-wrap gap-4">
      <% @insights.each do |insight| %>
        <%
          card_classes = case insight[:variant]
                         when :warning
                           "bg-amber-100 border-amber-300 text-amber-800"
                         when :success
                           "bg-emerald-50 border-emerald-200 text-emerald-700"
                         else
                           "bg-teal-50 border-teal-200 text-teal-700"
                         end
        %>
        <div class="flex items-center gap-3 px-4 py-3 rounded-lg border <%= card_classes %> flex-1 min-w-[200px]">
          <span class="text-lg flex-shrink-0" aria-hidden="true"><%= insight[:icon] %></span>
          <span class="text-sm font-medium">
            <% if insight[:link_path].present? %>
              <%= link_to insight[:message], insight[:link_path], class: "underline hover:no-underline", data: { turbo_frame: "_top" } %>
            <% else %>
              <%= insight[:message] %>
            <% end %>
          </span>
        </div>
      <% end %>
    </div>
  </section>
<% end %>
