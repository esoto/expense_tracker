<% content_for :title, t("expenses.titles.new") %>

<div class="max-w-2xl mx-auto">
  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-slate-900"><%= t("expenses.new.heading") %></h1>
      <p class="text-slate-600 mt-2"><%= t("expenses.new.subheading") %></p>
    </div>

    <%= render "form", expense: @expense %>
  </div>
</div>