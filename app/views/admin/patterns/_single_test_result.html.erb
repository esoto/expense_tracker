<div class="mt-4 p-3 rounded-lg <%= matches ? 'bg-emerald-50 border border-emerald-200' : 'bg-rose-50 border border-rose-200' %>">
  <div class="flex items-center">
    <% if matches %>
      <svg class="w-5 h-5 text-emerald-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span class="text-sm font-medium text-emerald-700"><%= t('admin.patterns.test_results.match_indicator') %></span>
    <% else %>
      <svg class="w-5 h-5 text-rose-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span class="text-sm font-medium text-rose-700"><%= t('admin.patterns.test_results.no_match_indicator') %></span>
    <% end %>
  </div>
  <% if test_text.present? %>
    <p class="text-xs text-slate-600 mt-2">
      <%= t('admin.patterns.test_results.test_text') %>: "<%= truncate(test_text, length: 50) %>"
    </p>
  <% end %>
</div>